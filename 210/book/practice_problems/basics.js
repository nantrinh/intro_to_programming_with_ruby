// 2
let num = 4936;
ones = num % 10;
tens = Math.floor(num / 10) % 10;
hundreds = Math.floor(num / 100) % 10;
thousands = Math.floor(num / 1000) % 10;
console.log(ones, tens, hundreds, thousands);

// 3
// String
// Boolean
// Number
// Number
// Undefined
// Object

// 5
// When you use the `+` operation and one of your operands is a string, the other operands are implicitly coerced to String, and the operands are concatenated.

// 6
console.log(parseInt('5') + 10);

// 7
console.log(`The value of 5 + 10 is ${Number('5') + 10}.`);

// 9
var names = ['asta', 'butterscotch', 'pudding', 'neptune', 'darwin'];
