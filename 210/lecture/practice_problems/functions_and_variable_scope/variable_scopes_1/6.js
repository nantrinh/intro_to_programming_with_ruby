console.log(a);

var a = 1;

// undefined
// because the code above is equivalent to
// ```
// var a;
// console.log(a);
// a = 1;
// ```
// JavaScript only hoists the variable declarations, not the
// assignments.
