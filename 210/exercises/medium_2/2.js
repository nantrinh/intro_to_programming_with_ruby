var person = { name: 'Victor' };
var otherPerson = { name: 'Victor' };

console.log(person.name === otherPerson.name);
// was false because strict equality checks to see whether the two objects are the same
