// 'hello from the function scope', reassigned on 4
// 'hello from the global scope', the variable declared on line 1 is unaffected by the reassignment within the function because strings are primitive values and thus immutable
